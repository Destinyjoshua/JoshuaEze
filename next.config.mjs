/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard Next.js config for Vercel (SSR + SSG)
  // Do not set output: 'export' unless you want a fully static site
};

export default nextConfig;